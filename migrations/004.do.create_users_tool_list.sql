CREATE TABLE users_tool_list (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER
    REFERENCES users(id) ON DELETE CASCADE NOT NULL,
  tool_id INTEGER 
    REFERENCES tools(id) ON DELETE CASCADE NOT NULL
);